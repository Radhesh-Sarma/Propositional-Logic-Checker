MODULE main

VAR
	state: {s0,s1,s2};

ASSIGN
	init(state) := (s0);

	next(state) := case
					state = s0: {s1,s2};
					state = s1: s1;
					state = s2: s0;
					esac;


DEFINE
	
	p := state = s1;
	q := state = s2;


CTLSPEC AF(p|q)
CTLSPEC AF p | AF q

